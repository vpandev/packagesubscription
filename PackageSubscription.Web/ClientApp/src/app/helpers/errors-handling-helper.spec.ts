import { ErrorsHandlingHelper } from './errors-handling-helper';

describe('ErrorsHandlingHelper', () => {
  it('should create an instance', () => {
    expect(new ErrorsHandlingHelper()).toBeTruthy();
  });
});
