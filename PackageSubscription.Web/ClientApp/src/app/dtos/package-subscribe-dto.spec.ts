import { PackageSubscribeDto } from './package-subscribe-dto';

describe('PackageSubscribeDto', () => {
  it('should create an instance', () => {
    expect(new PackageSubscribeDto()).toBeTruthy();
  });
});
